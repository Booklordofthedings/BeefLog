# BeefLog
My personal logging library for the beef programming language
Allows you to log by just calling the Logger.Log() function
The normal version just uses the default settings otherwise you can input extra additional settings
The Get and Set functions can change these default inputs.
